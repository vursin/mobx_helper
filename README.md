# Mobx Helper

Make Mobx easier to use with some extensions.

## Usage

``` dart
// Easy to declare
final text = ''.asObs;

// Change value for variable
text('new text');

// Get value from variable
final value = text();
```
